# 🧬 Atomic Playground — Інтерактивна 3D Платформа для Візуалізації Атомів та Молекул

## 🚀 Опис проєкту
**Atomic Playground** — сучасний освітній веб-додаток для демонстрації будови атомів, валентності та формування молекул.  
Додаток побудований на **SOLID принципах**, модульній архітектурі, використанні програмних патернів та найкращих UI/UX практик.  
Підтримуються **desktop та mobile layout-и**, окремо спроєктовані для різних розмірів екранів.

---

## 🧱 Технологічний стек

- **React 18+**, **TypeScript**
- **Vite**
- **React Three Fiber + Three.js** для 3D
- **Tailwind CSS**
- **Framer Motion** — анімації
- **Zustand / Redux Toolkit**
- **i18next** — локалізація
- **loglevel / Winston wrapper** — гнучке логування
- **LocalStorage persistence** для збереження налаштувань

---

## 📱 Desktop & Mobile Layout (розділені)
- Дві окремі системи верстки:
  - **Desktop View**: широкі панелі інформації, горизонтальна структура, більше контролів.
  - **Mobile View**: вертикальний флоу, спрощені панелі, оптимізовані 3D-взаємодії.
- UI адаптується, але компоненти для обох режимів **окремо організовані**, щоб уникнути надмірної складності.

```
src/
  layouts/
    desktop/
    mobile/
```

---

## ⚙️ Система налаштувань (Settings System)
### Користувач може змінювати:
- тему (**light / dark / system**)
- мову інтерфейсу (через i18next)
- рівень логування (debug/info/warn/error)
- швидкість анімацій
- режим відображення атома (реалістичний / спрощений)

### Збереження:
Налаштування зберігаються у **LocalStorage** через абстракцію:

```
SettingsService -> ISettingsProvider -> LocalSettingsProvider
```

Підтримує розширення: можна легко додати новий провайдер, напр. IndexedDB.

---

## 🌐 Локалізація
Використовується **i18next**.

### Додавання нової мови:
1. Створити файл:  
   `src/i18n/<language>.json`
2. Додати у конфіг:  
   `i18n.config.ts`

Готово — UI підтягне все автоматично.

---

## 📝 Логування
Архітектура логування базується на патерні **Adapter**.

### Можливості:
- Вмикати/вимикати логування для окремих модулів
- Різні рівні логів: `debug | info | warn | error`
- Продакшн режими автоматично приховують зайві логи

Конфіг: `src/config/logger.config.ts`

---

## 🎨 UI / UX Найкращі практики

### Впроваджено:
- чітка ієрархія типографії
- коректні відступи, сітки, spacing scale
- мікроанімації для інтерактивних елементів
- tooltips та пояснення термінів
- колір-коди для елементів атома
- плавні переходи між сценами
- фокус на accessibility (контраст, ARIA-атрибути)

---

## 📂 Архітектура (SOLID-first)

```
src/
  core/
    models/           ← інтерфейси, структури даних
    services/
      AtomService.ts
      MoleculeService.ts
      SettingsService.ts
      LoggerService.ts
      LocalizationService.ts
  modules/
    atom-view/
    molecule-view/
    home/
  layouts/
    desktop/
    mobile/
  i18n/
    en.json
    ua.json
  state/
  config/
  utils/
```

### Уся логіка розбита згідно SOLID:
- **S** — кожен сервіс робить тільки свою функцію  
- **O** — система розширюється додаванням нових файлів  
- **L** — інтерфейси дозволяють заміну реалізацій  
- **I** — дрібні інтерфейси без надлишкових функцій  
- **D** — компоненти залежать від абстракцій, а не реалізацій  

---

## 🧪 Тестування
- **Unit-тести** моделей
- **UI snapshot tests**
- **Smoke tests** 3D сцени

---

## 🛠 Запуск

```
npm install
npm run dev
npm run build
npm run preview
```

### CI / CD (GitHub Pages)

- Автоматична збірка й деплой запускаються на push у `main` або `master`.
- Базовий шлях Vite налаштовується змінною `BASE_PATH` (workflow встановлює `/<repo>/`).
- Артефакт збірки завантажується та публікується через GitHub Pages (`.github/workflows/deploy.yml`).

---

## 🎯 Мета

Створити масштабований, гнучкий, легко підтримуваний та розширюваний застосунок, який демонструє атоми та молекули у спосіб, зрозумілий навіть школярам, але достатньо технічний для університетського рівня.

---

